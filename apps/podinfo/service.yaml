apiVersion: v1
kind: Service
metadata:
  name: podinfo-team-2
  namespace: team-2
spec:
  type: ClusterIP
  selector:
    app: podinfo-team-2
  ports:
    - name: http
      port: 9898
      protocol: TCP
      targetPort: http
    - port: 9999
      targetPort: grpc
      protocol: TCP
      name: grpc
